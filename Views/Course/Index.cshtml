@{
    ViewData["Title"] = "Welcome to SkillForge!";
}
@model List<quiz_project.ViewModels.CourseViewModel>

<div class="container">
    <div class="d-flex justify-content-between align-items-center">
        <h1>@ViewData["Title"]</h1>

        <a class="btn btn-primary text-white" asp-area="" asp-controller="Course" asp-action="Create">
            Create course
        </a>
    </div>
</div>

<div class="container">
    <div class="row align-items-start">
        @{
            await Html.RenderPartialAsync("~/Views/Shared/_CourseDetails.cshtml");
        }
    </div>
</div>
